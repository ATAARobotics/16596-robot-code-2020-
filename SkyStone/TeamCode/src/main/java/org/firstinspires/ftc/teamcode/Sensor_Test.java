package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.eventloop.opmode.TeleOp;
import com.qualcomm.robotcore.util.ElapsedTime;

//@TeleOp(name="Sensor_Test", group="TeleOp")

//public class Sensor_Test extends LinearOpMode{
    //private ElapsedTime runtime = new ElapsedTime();
    //private RobotInterface robotui = null;

    //@Override
    //public void runOpMode() {
        //robotui = new RobotInterface(hardwareMap, telemetry, false, false, false);
       // telemetry.addData("Status", "Initialized");
        //telemetry.update();

         //Wait for the game to start (driver presses PLAY)
        //waitForStart();
        //runtime.reset();

         //run until the end of the match (driver presses STOP)
        //while (opModeIsActive()) {
          //  telemetry.addData("Color sensor red", colorSensor.red());
        //    telemetry.addData("Color sensor green", colorSensor.green());
      //      telemetry.addData("Color sensor blue", colorSensor.blue());
    //        telemetry.addData("Color sensor alpha", colorSensor.alpha());
  //          telemetry.addData("Color sensor argb", colorSensor.argb());
//            telemetry.update();

        //}
    //}
//}
